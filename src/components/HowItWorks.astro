---
import { LinkIcon, FlagIcon, Zap, Shield } from "lucide-react";

const steps = [
  {
    step: "01",
    title: "Import Your Config",
    description:
      "Paste any V2Ray subscription link or JSON configuration. Marz intelligently parses both formats, extracting server details, protocols, and connection parameters automatically.",
    icon: "link",
  },
  {
    step: "02",
    title: "Smart Server Detection",
    description:
      "Marz reads the remarks field and identifies country flags (🇺🇸, 🇩🇪, 🇯🇵) to categorize servers geographically. No manual organization needed—your servers are instantly mapped and grouped.",
    icon: "flag",
  },
  {
    step: "03",
    title: "Health Verification",
    description:
      "Before you connect, Marz pings each server to verify availability and measure latency. Dead servers are flagged, and the fastest options are highlighted for optimal performance.",
    icon: "zap",
  },
  {
    step: "04",
    title: "One-Tap Connection",
    description:
      "Select a server and tap connect. Marz handles protocol negotiation, encryption setup, and tunnel establishment—all behind a clean, intuitive interface.",
    icon: "shield",
  },
];

const configExample = `vless://uuid@server.com:443?type=tcp&security=tls#🇺🇸 US Server
vmess://eyJhZGQiOiJzZXJ2ZXIuY29tIiwicG9ydCI6IjQ0MyIsInBzIjoi8J+HqfCfh6ogREUgU2VydmVyIn0=`;
---

<section id="how-it-works" class="py-20">
  <div class="text-center mb-16 px-6">
    <h2 class="text-4xl font-medium tracking-tight mb-4">How Marz Works</h2>
    <p class="text-muted-foreground max-w-2xl mx-auto text-lg">
      From subscription link to secure connection in seconds. Marz handles the
      complexity so your users don't have to.
    </p>
  </div>

  <div class="grid grid-cols-1 lg:grid-cols-2 gap-0 border-y border-border">
    <!-- Left: Steps -->
    <div class="border-r border-border">
      {
        steps.map((step, index) => (
          <div
            class:list={[
              "p-8 flex gap-6",
              index < steps.length - 1 && "border-b border-border",
            ]}
          >
            <div class="shrink-0">
              <span class="text-4xl font-bold text-primary/20">
                {step.step}
              </span>
            </div>
            <div>
              <h3 class="text-lg font-semibold mb-2">{step.title}</h3>
              <p class="text-muted-foreground text-sm leading-relaxed">
                {step.description}
              </p>
            </div>
          </div>
        ))
      }
    </div>

    <!-- Right: Visual representation -->
    <div class="flex flex-col">
      <!-- Config parsing visualization -->
      <div class="p-8 border-b border-border flex-1">
        <h4 class="text-sm font-medium text-muted-foreground mb-4">
          Supported Input Formats
        </h4>
        <div class="space-y-3">
          <div class="bg-muted/50 rounded-lg p-4 font-mono text-xs break-all">
            <div class="text-primary">vless://</div>
            <span class="text-muted-foreground"
              >uuid@server.com:443?type=tcp&security=tls</span
            >
            <span class="text-foreground font-semibold">#🇺🇸 US Server</span>
          </div>
          <div class="bg-muted/50 rounded-lg p-4 font-mono text-xs break-all">
            <div class="text-primary">vmess://</div>
            <span class="text-muted-foreground"
              >eyJhZGQiOiJzZXJ2ZXIuY29tIiwi...</span
            >
            <span class="text-foreground font-semibold">#🇩🇪 DE Server</span>
          </div>
          <div class="bg-muted/50 rounded-lg p-4 font-mono text-xs">
            <span class="text-primary">{"{"}</span>
            <span class="text-muted-foreground">"outbounds": [...]</span>
            <span class="text-primary">{"}"}</span>
            <span class="text-foreground/60 ml-2">JSON Config</span>
          </div>
        </div>
      </div>

      <!-- Protocol support -->
      <div class="p-8 flex-1">
        <h4 class="text-sm font-medium text-muted-foreground mb-4">
          Protocol Support
        </h4>
        <div class="grid grid-cols-2 gap-3">
          {
            [
              "VLESS",
              "VMess",
              "Trojan",
              "Shadowsocks",
              "REALITY",
              "WireGuard",
            ].map((protocol) => (
              <div class="flex items-center gap-2 bg-muted/30 rounded-lg px-4 py-3">
                <div class="w-2 h-2 rounded-full bg-primary" />
                <span class="text-sm font-medium">{protocol}</span>
              </div>
            ))
          }
        </div>
        <div class="mt-6 p-4 bg-primary/5 rounded-lg border border-primary/10">
          <p class="text-sm text-muted-foreground">
            <span class="font-medium text-foreground">Pro tip:</span> Use country
            flag emojis in your server remarks (e.g., <code class="text-primary"
              >🇺🇸 US-1</code
            >) for automatic geographic grouping in the app.
          </p>
        </div>
      </div>
    </div>
  </div>
</section>
