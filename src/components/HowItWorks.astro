---
import { LinkIcon, FlagIcon, Zap, Shield } from "lucide-react";

const steps = [
  {
    step: "01",
    title: "Import Your Config",
    description:
      "Import universal subscription link. Marz parses both formats (v2ray links and json), extracting server details, protocols, and connection parameters automatically.",
    icon: "link",
  },
  {
    step: "02",
    title: "Smart Server Detection",
    description:
      "Marz reads the remarks field and identifies country flags (🇺🇸, 🇩🇪, 🇯🇵) to categorize servers geographically. No manual organization needed—your servers are instantly mapped and grouped.",
    icon: "flag",
  },
  {
    step: "03",
    title: "One-Tap Connection",
    description:
      "Select a server and tap connect. Marz pings each server to verify availability and measure latency. Dead servers are flagged, and the fastest options are highlighted for optimal performance.",
    icon: "shield",
  },
];

const configExample = `vless://uuid@server.com:443?type=tcp&security=tls#🇺🇸 US Server
vmess://eyJhZGQiOiJzZXJ2ZXIuY29tIiwicG9ydCI6IjQ0MyIsInBzIjoi8J+HqfCfh6ogREUgU2VydmVyIn0=`;
---

<section id="how-it-works" class="pt-20">
  <div class="text-center mb-16 px-6">
    <h2 class="text-4xl font-medium mb-4">How Marz Works</h2>
    <p class="max-w-2xl mx-auto text-muted-foreground text-center">
      From subscription link to secure connection in seconds. Marz handles the
      complexity so your users don't have to.
    </p>
  </div>

  <div class="grid grid-cols-1 lg:grid-cols-2 gap-0 border-y border-border">
    <!-- Left: Steps -->
    <div class="lg:border-r">
      {
        steps.map((step, index) => (
          <div
            class:list={[
              "p-8 flex gap-6",
              index < steps.length - 1 && "border-b border-border",
            ]}
          >
            <div class="shrink-0">
              <span class="text-4xl font-bold text-primary/20">
                {step.step}
              </span>
            </div>
            <div>
              <h3 class="text-lg font-semibold mb-2">{step.title}</h3>
              <p class="text-muted-foreground text-sm leading-relaxed">
                {step.description}
              </p>
            </div>
          </div>
        ))
      }
    </div>

    <!-- Right: Visual representation -->
    <div class="flex flex-col border-t lg:border-t-0" id="deep-linking">
      <!-- Config parsing visualization -->
      <div class="p-8 pb-0 flex-2/3">
        <h4 class="font-medium mb-4">Deep linking guide</h4>
        <div class="">
          <p class="text-muted-foreground text-sm leading-relaxed">
            Marz leverages deeplinking to implement easy auto import
            subscription urls. It helps users to one-tap import subscriptions.
          </p>
          <h4 class="font-medium text-muted-foreground text-sm mb-4 mt-10">
            Schema format
          </h4>
          <p class="font-mono tracking-normal bg-primary-">
            <span class="text-primary">marz://import?</span><span
              class="text-primary/85">url=</span
            ><span
              class="underline decoration-dashed underline-offset-3 opacity-75"
              >https%3A%2F%2Fmarz.surf%2Fsub%2Ffree-net</span
            ><span class="text-primary/85">&name=</span><span
              class="underline decoration-dashed underline-offset-3 opacity-75"
              >marz</span
            >
          </p>
          <h4 class="font-medium text-muted-foreground text-sm mb-4 mt-10">
            Parameters:
          </h4>
          <ul class="flex flex-col gap-2 font-mono text-sm">
            <li class="whitespace-pre">• url : URI Encoded Subscription URL</li>
            <li class="whitespace-pre">• name: URI Encoded Provider Name</li>
          </ul>
        </div>
      </div>

      <!-- Protocol support -->
      <div class="p-8 flex-1/3">
        <div class="p-4 bg-primary/5 border border-primary/10">
          <p class="text-sm text-muted-foreground">
            <span class="font-medium text-foreground">Pro tip:</span> Use country
            flag emojis in your server remarks (e.g., <code class="text-primary"
              >🇺🇸 US-1</code
            >) for automatic geographic grouping in the app.
          </p>
        </div>
      </div>
    </div>
  </div>
</section>
